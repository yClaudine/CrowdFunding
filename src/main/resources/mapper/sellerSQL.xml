<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="seller">
	<insert id="insertSeller" parameterType="s">
		insert into seller values(seller_seq.nextval,#{memberId},#{enNo},#{enPath},#{enType},#{enIntro},#{owner},#{photopath},#{position},#{address},#{authSeller},null)
			
	</insert>
  
</mapper>
