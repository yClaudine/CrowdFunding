<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="storeDelivery">
	<insert id="insertDelivery" parameterType="sd">
		INSERT INTO store_delivery VALUES
		(STORE_DELEIVERY_SEQ.nextval,#{storeNo},#{memberNo},#{storepayNo},#{storeDelivery},#{storeProduct},#{storeProductnum},#{deliveryName},#{deliveryPhone},#{deliveryAddress},#{deliveryMsg})
	</insert>
</mapper>
